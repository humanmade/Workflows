'use strict';

var _require = require('react-app-rewired'),
    injectBabelPlugin = _require.injectBabelPlugin;

function rewireStyledComponents(config, env) {
  var styledComponentsPluginOptions = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};

  return injectBabelPlugin(['styled-components', styledComponentsPluginOptions], config);
}

module.exports = rewireStyledComponents;